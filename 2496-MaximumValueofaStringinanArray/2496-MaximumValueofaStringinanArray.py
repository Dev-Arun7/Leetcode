                    _isAlpha = True
                        value = len(i)
            else:
                if int(i) > value:
                if j.isalpha():
            for j in i:
        for i in strs:
        value = 0
            _isAlpha = False
    def maximumValue(self, strs: List[str]) -> int:
class Solution:
                    break
            if _isAlpha:
                if len(i) > value:
                    value = int(i)           
        return value
        
[
