            else:
        return flag
                return False

                continue
            if (i in s) and (t.count(i) <= s.count(i)):
        for i in t:
        if len(s) != len(t):
        flag = True
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
            return False
                
        
"
