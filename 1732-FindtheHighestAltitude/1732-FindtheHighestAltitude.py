class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        altitude = [0]
        for i in gain:
            temp += i
        temp = 0
            altitude.append(temp)
        return max(altitude)
        
[
