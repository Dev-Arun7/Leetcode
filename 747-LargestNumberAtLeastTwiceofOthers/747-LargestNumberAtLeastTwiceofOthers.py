        for i, num in enumerate(nums):
            if num > first:
                first = num
            elif num > second:
                second = num
        
        if first >= 2 * second:
                index = i
            return index
        index = 0
        second = float('-inf')
        else:
        first = float('-inf')
    def dominantIndex(self, nums: List[int]) -> int:
class Solution:
                second = first
[
