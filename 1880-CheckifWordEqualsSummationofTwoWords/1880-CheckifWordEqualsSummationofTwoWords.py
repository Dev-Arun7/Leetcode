        for i in firstWord:
            k=k+str(h.index(i))
        for i in secondWord:
            a+=str(h.index(i))
        for i in targetWord:
            e+=str(h.index(i))
        if int(k)+int(a)==int(e):
            return True
        else:
        h=string.ascii_lowercase
        a=''
        e=''
        import string
        k=''
class Solution:
    def isSumEqual(self, firstWord: str, secondWord: str, targetWord: str) -> bool:
"
