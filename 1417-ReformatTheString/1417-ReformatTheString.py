                if i != l-1:
                    ans += letters[i] + digits[i]
                else:
                    ans += letters[i]
            for i in range(d):
                if i != d-1:
            for i in range(l):
        elif l > d:
            return ans
        exist = [-1, 0, 1]
        if (l-d) not in exist:
        elif l == d:
        else:
            for i in range(l):
                ans += letters[i] + digits[i]
                    ans += digits[i] + letters[i]
                else:
                    ans += digits[i]
        return ans

        
"
