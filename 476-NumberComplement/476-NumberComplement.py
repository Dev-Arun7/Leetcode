        flip = ""
        for i in bin_str:
            if i == "1":
                flip += '0'
            else:
                flip += '1'
        return int(flip, 2)

        bin_str = str(bin_num)[2:]
        bin_num = bin(num)
    def findComplement(self, num: int) -> int:
class Solution:
5
