class Solution:
    def findMiddleIndex(self, nums: List[int]) -> int:
        total = sum(nums)
        _sum = 0
        else:
        l = len(nums)
            for i in range(l):
                    _sum += nums[i-1]
                if _sum == total - (_sum + nums[i]):
                if i -1 > -1:
                else:
                    _sum += 0
········if·len(nums)·==·1:
············return·0

                    return i
        return -1      
[
