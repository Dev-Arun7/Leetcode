class Solution:
    def removeStars(self, s: str) -> str:
        ans = []
        for letter in s:
                ans.append(letter)
        return ans
            if letter == '*':
                ans.pop()
            else:
        ans = ''.join(ans)
        
"
