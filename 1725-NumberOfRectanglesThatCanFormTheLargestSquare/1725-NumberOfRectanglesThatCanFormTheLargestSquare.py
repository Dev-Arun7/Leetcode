class Solution:
    def countGoodRectangles(self, rectangles: List[List[int]]) -> int:
        ans = 0
        for i in rectangles:
            temp = min(i)
            if temp > ans:
        return count
        count = 0
                ans = temp
                count = 1
            elif temp == ans:
                count += 1
        
[
